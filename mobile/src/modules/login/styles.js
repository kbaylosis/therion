import { StyleSheet } from "react-native";
import { white } from "__src/config/styles";

export default StyleSheet.create({
	container: {
		flex: 1,
		backgroundColor: white,
		alignItems: "center",
		justifyContent: "center",
	},
});
